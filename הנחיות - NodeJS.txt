// ליצור קובץ ראשי
app.js

npm init -y

// כל ההתקנות ביחד
npm install express cors mongoose moment morgan chalk joi jsonwebtoken bcrypt fs formidable --save

// הוספה לקובץ package.json (מתחת main)
"type": "module",

// ליצור קובץ .gitignore
// להוסיף את השורות הבאות:
/node_modules
/package-lock.json

// התקנה חד"פ גלובלית
npm i -g nodemon -D